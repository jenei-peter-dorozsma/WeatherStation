<StackLayoutBox@StackLayout>:
    spacing: 5
    padding: 10

    canvas.before:
        Color:
            rgba: 0, 1, 0, 1  # Green color
        Line:
            rectangle: (self.x, self.y, self.width, self.height)
            width: 2
        